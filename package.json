{
  "name": "fragment-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Plugin Agent pour Fragment",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --format=cjs --external:fragment --outfile=main.js",
    "dev": "npm run build -- --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run src"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2",
    "vitest": "^5.0.2"
  }
}
