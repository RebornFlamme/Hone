{
	"name": "fragment-scan",
	"version": "0.1.0",
	"description": "Scanne une feuille avec le téléphone et la range dans le vault Fragment.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [
		"fragment",
		"plugin"
	],
	"author": "OscarFLasky",
	"license": "MIT",
	"devDependencies": {
		"@codemirror/state": "^6.4.1",
		"@types/node": "^20.11.0",
		"@usefragment/core": "^0.1.0",
		"builtin-modules": "^3.3.0",
		"esbuild": "^0.21.5",
		"tslib": "^2.6.3",
		"typescript": "~5.9.3"
	},
	"dependencies": {
		"qr-code-styling": "^1.9.2"
	}
}
